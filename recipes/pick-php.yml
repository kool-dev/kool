actions:
  # Defines which app service to use (PHP version)
  - prompt: 'Which app service do you want to use'
    default: 'PHP 8.0'
    options:
      - name: 'PHP 8.0'
        actions:
          - recipe: php-8
      - name: 'PHP 7.4'
        actions:
          - recipe: php-7.4
