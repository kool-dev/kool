scripts:
  # bun - helpers for JS handling
  bun: kool docker --user $KOOL_ASUSER --env HOME=/tmp oven/bun:1 bun
  bunx: kool docker --user $KOOL_ASUSER --env HOME=/tmp oven/bun:1 bunx
  before-start:
    - kool run bun install
  reset:
    - kool run bun install
