scripts:
  # node
  node: kool exec app node
  npm: kool exec app npm
  adonis: kool exec app adonis

  # examples
  install:
    - cp .env.example .env
    - kool run npm install
    - kool start # this is required as the first start may not have node_modeules and crash
